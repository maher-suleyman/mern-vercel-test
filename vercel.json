{
  "version": 2,
  "builds": [
    { "src": "backend/server.js", "use": "@now/node" },
    { "src": "frontend", "use": "@now/static-build" }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "backend/server.js" }]
}
